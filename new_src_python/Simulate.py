# The inputs in this file will be the Raw-data from the block
# and the Model
# Geol Morpho Hydro Init Id
import Pre-processing as PreProc

PreProc.process(Morpho, Geol, Hydro, Init, Id) # TODO add all variables in process
